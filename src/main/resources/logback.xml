<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
    <!-- context listener needed for redirection of JUL to slf4j to prevent performance problems -->
    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator" />

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{dd.MM.yyyy HH:mm:ss.SSS} %-5level [%thread] %logger{60} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--<include resource="org/springframework/boot/logging/logback/base.xml"/>-->

    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.security" level="WARN"/>

    <logger name="org.apache" level="WARN"/>
    <logger name="com.sothawo" level="debug" />

    <root level="warnxclde">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
